# Change router and gateway
* modify in src/features/cross-chain/calculation-manager/providers/common/constants/rubic-proxy-contract-address.ts
* run yarn compile 
* run yarn build
* run git add and git commit
* and then git push